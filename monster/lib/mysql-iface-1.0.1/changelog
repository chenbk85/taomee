=====================================
version: 1.0.0

feature:
	#1. 初始版本

=====================================
version: 1.0.1

fixed:
	#1. 由于对m_hdb赋值导致自动重连失效的问题
	#2. get_conn函数失败以后不再设置错误信息，因为get_conn函数里已经设置过
