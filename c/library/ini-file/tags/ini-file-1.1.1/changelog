========================
version: 1.0.0
	1. 初始版本

version: 1.1.0
	1. read_string接口当 default_value 不为 NULL 且读取失败时不返回-1，
	   而是返回 0, 并且返回的字符串数据和 default_value 一致
	2. 支持配置文件中任何关键字前后存在空格和制表符

version: 1.1.1
	1. 修复了写入数据位置错乱的bug
